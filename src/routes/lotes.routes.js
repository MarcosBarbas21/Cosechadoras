const { Router } = require('express')
const router = Router()

const { } = Router('../controllers/lotes.controllers')



/*  
    añadir los get y post de los lotes
*/ 


module.exports = router